name: Project CI

on:
  push:
    branches: [main, dev]
  pull_request:
    branches: [main]

jobs:
  frontend:
    name: Frontend Job
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Generate Prisma client
        run: bunx prisma generate
        env:
          DATABASE_URL: "mysql://user:pass@localhost:3306/db"

      - name: Run frontend tests
        run: bunx vitest run tests/test_cart_ui.test.tsx tests/test_filter.test.ts tests/test_finance_ui.test.tsx
        env:
          JWT_SECRET: "test-secret-for-ci"

  backend:
    name: Backend Job
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Generate Prisma client
        run: bunx prisma generate
        env:
          DATABASE_URL: "mysql://user:pass@localhost:3306/db"

      - name: Run backend tests
        run: bunx vitest run tests/test_auth.test.ts tests/test_checkout.test.ts tests/test_cart.test.ts tests/test_transactions.test.ts tests/test_validators.test.ts
        env:
          JWT_SECRET: "test-secret-for-ci"

      - name: Upload coverage report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: coverage-report
          path: coverage/
          retention-days: 7
          if-no-files-found: ignore
